compile = "make"
run = "make install make dev make build make lint make format make clean"
modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

[deployment]
build = ["sh", "-c", "make"]
run = ["sh", "-c", "make install make dev make build make lint make format make clean"]

[[ports]]
localPort = 3000
externalPort = 80
